version: '1.0'

steps:
  java_build:
    type: build
    working_directory: ${{main_clone}}
    description: Building with gradle...
    image: gradle:jdk8
    command:
      - ./gradlew --debug deployNodes

version: '1.0'

steps:
  java_build:
    working_directory: ${{main_clone}}
    description: Building with gradle...
    image: gradle:jdk8
    image_name: labsji/yo_cordapp
    command:
      - gradle --debug deployNodes
